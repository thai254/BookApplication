<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:gravity="bottom"
    android:padding="16dp"
    android:background="@drawable/nav_header2"
    android:id="@+id/nav_header"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <FrameLayout
        android:layout_width="103dp"
        android:layout_height="103dp"
        android:background="@drawable/custom_user_image">
    <ImageView
        android:id="@+id/img_user"
        android:layout_margin="1dp"
        android:layout_width="101dp"
        android:layout_height="101dp"
        android:scaleType="centerCrop"
        android:src="@drawable/logo_book"/>
    </FrameLayout>

    <FrameLayout
        android:layout_marginTop="100dp"
        android:layout_width="match_parent"
        android:layout_height="90dp">
    <TextView
        android:id="@+id/txt_username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="30dp"
        android:fontFamily="@font/handofseanproiciel_regular"
        android:gravity="left"
        android:text="FLY BOOK"
        android:textSize="18sp"
        android:textColor="@color/lavender"
        android:textStyle="bold"
        android:layout_marginTop="5dp"/>
        <TextView
            android:id="@+id/txt_useremail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="30dp"
            android:text="contact@flybook.com"
            android:textColor="@color/lavender"
            android:textSize="14sp"/>

        <ImageButton
            android:id="@+id/Change_username"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:background="@android:color/transparent"
            android:layout_marginTop="10dp"
            app:srcCompat="@drawable/edit" />
    </FrameLayout>
</FrameLayout>